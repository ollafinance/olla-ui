apiVersion: v1
kind: Service
metadata:
  name: olla-ui
  labels:
    app: olla-ui
    app.kubernetes.io/name: olla-ui
    app.kubernetes.io/part-of: olla
spec:
  type: ClusterIP
  selector:
    app: olla-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
