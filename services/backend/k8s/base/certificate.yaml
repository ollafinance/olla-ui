apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: olla-indexer-tls
spec:
  dnsNames:
    - api.demo.olla.finance
  issuerRef:
    kind: ClusterIssuer
    name: olla-issuer-production
  secretName: olla-indexer-tls
